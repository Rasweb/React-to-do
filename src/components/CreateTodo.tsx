export const CreateTodo = () => {
  return (
    <>
      {/* <form>
        <input type="text" value={title} onChange={handleCreate} />
        <input type="number" value={duration} onChange={handleCreate} />
        <input type="text" value={done} onChange={handleCreate} />
        <input type="number" value={id} onChange={handleCreate} />
        <button type="submit">Submit</button>
      </form> */}
    </>
  );
};
